function solution(n_str) {
    // 방법 1: Number() 사용 (가장 직관적)
    return Number(n_str);
    
    // 방법 2: + 연산자 사용 (가장 짧음)
    // return +n_str;
    
    // 방법 3: parseInt() 사용
    // return parseInt(n_str, 10);
}
